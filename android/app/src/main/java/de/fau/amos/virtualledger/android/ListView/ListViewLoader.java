/*
package de.fau.amos.virtualledger.android.ListView;

import android.app.ListActivity;
import android.app.ListFragment;
import android.app.LoaderManager;
import android.content.CursorLoader;
import android.content.Loader;
import android.database.Cursor;
import android.os.Bundle;

import java.util.ArrayList;
import java.util.List;

import de.fau.amos.virtualledger.android.menu.adapter.BankAccessListAdapter;
import de.fau.amos.virtualledger.android.menu.model.BankAccess;

*/
/**
 * Created by Simon on 20.05.2017.
 *//*


public class ListViewLoader extends ListFragment implements LoaderManager.LoaderCallbacks<Cursor> {

    BankAccessListAdapter bankAccessAdapter;
    List<BankAccess> bankAccesses = new ArrayList<BankAccess>();

    int size = bankAccessAdapter.getCount();
    static final String[] displayingElements = new String[size];
    for(element:displayingElements) {

    }

    @Override
    public Loader<Cursor> onCreateLoader(int id, Bundle args) {
        return null;
    }


    @Override
    public void onLoadFinished(Loader<Cursor> loader, Cursor data) {

    }

    @Override
    public void onLoaderReset(Loader<Cursor> loader) {

    }
}
*/
